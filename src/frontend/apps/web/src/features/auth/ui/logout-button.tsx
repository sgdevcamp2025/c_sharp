import { Button } from '@workspace/ui/components';

const LogoutButton = () => {
  return (
    <Button
      variant="outline"
      size="sm"
      className="w-full text-xs"
    >
      LOGOUT
    </Button>
  );
};
export default LogoutButton;
